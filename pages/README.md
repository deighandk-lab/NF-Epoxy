# NF Epoxy pages

The current production-style architecture contains:

- `about.html`
- `products.html`
- `services.html`
- `workshops.html`

There is intentionally no Contact page. WhatsApp is the primary enquiry method.

All pages use the shared stylesheet and JavaScript in `../assets/` and include the site-wide WhatsApp floating button.